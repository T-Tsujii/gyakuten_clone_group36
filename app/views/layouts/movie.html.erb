require 'csv'

CSV.output do |csv|
  csv_column_names = ["genre","title","url"]
  csv << csv_column_names
  @movies.each do |movie|
    csv_column_values = [
      movie.genre,
      movie.title,
      movie.url,
    ]
    csv << csv_column_values
  end
end
